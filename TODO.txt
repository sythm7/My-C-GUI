- Créer un thread par fenetre. (Les events sont gérés par un seul thread donc une seul fenêtre gère les events de plusieurs fenêtres) -> En cours (dans la branche testThreads)
    Trouver un moyen 

(+ Résoudre bug crash quand on ferme et qu'on reouvre plein de fenetres d'afilée) -> Si les threads fonctionnent plus besoin

- Détruire tous les évènements ayant été ajoutés aux composants lors de leur destruction (créer une liste de tous les évènements ajoutés pour tous les détruire)

